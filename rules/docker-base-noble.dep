
DPATH       := $($(DOCKER_BASE_NOBLE)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-base-noble.mk rules/docker-base-noble.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_BASE_NOBLE)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_BASE_NOBLE)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_BASE_NOBLE)_DEP_FILES   := $(DEP_FILES)
