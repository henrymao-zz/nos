
DPATH       := $($(DOCKER_SWSS_LAYER_JAMMY)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-swss-layer-jammy.mk rules/docker-swss-layer-jammy.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_SWSS_LAYER_JAMMY)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_SWSS_LAYER_JAMMY)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_SWSS_LAYER_JAMMY)_DEP_FILES   := $(DEP_FILES)
