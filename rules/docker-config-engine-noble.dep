
DPATH       := $($(DOCKER_CONFIG_ENGINE_NOBLE)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-config-engine-noble.mk rules/docker-config-engine-noble.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_CONFIG_ENGINE_NOBLE)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_CONFIG_ENGINE_NOBLE)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_CONFIG_ENGINE_NOBLE)_DEP_FILES   := $(DEP_FILES)
